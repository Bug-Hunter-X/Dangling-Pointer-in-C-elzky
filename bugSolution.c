int main() {    int x = 10;    int *ptr = &x;    *ptr = 20;    printf("%d\n", x); //Output:20    int y = 30;    ptr = &y;    *ptr = 40;    printf("%d\n", x); //Output:20    //To avoid dangling pointers, ensure that when you reassign the pointer, you are not using the original pointer again. In this case, you should not use ptr to access the memory that x was pointing to after ptr is reassigned.    return 0;} 